# Updated CI Configuration

# This configuration adds Authorization headers with test-bypass token to all smoke test curl requests for the specified endpoints.

"""
# Add the following lines to include the Authorization header

# For /api/inference endpoint
curl -H 'Authorization: Bearer test-bypass' http://your-api-url/api/inference

# For /api/outcome endpoint
curl -H 'Authorization: Bearer test-bypass' http://your-api-url/api/outcome

# For /api/simulate endpoint
curl -H 'Authorization: Bearer test-bypass' http://your-api-url/api/simulate
"""
