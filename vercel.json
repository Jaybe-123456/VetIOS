{
    "framework": "nextjs",
    "installCommand": "corepack pnpm install --frozen-lockfile",
    "buildCommand": "cd ../.. && corepack pnpm run build --filter=@vetios/web",
    "outputDirectory": ".next"
}