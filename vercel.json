{
    "$schema": "https://openapi.vercel.sh/vercel.json",
    "buildCommand": "pnpm run build --filter=@vetios/web",
    "installCommand": "corepack enable && pnpm install",
    "framework": "nextjs",
    "outputDirectory": "apps/web/.next"
}